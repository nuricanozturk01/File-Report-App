namespace FileReporterApp.ServiceApp.options
{
    public enum OtherOptions
    {
        EMPTY_FOLDERS, NTFS_PERMISSIONS, OVERWRITE
    }

    public enum TimeEnum
    {
        BEFORE, AFTER
    }

    public enum FileType
    {
        TEXT, EXCEL
    }

    public enum DateOptions
    {
        CREATED, MODIFIED, ACCESSED
    }

    public enum BasicOptions
    {
        SCAN, MOVE, COPY
    }
}